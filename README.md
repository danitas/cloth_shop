## Start the application

Application is using docker to build the environment to run it you need to install docker locally

After installation the command: `docker-compose up --build` should be invoked

To clear docker compose the next command need to be run: `docker-compose down --volumes --rmi all`